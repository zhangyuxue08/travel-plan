# 2026 国庆新西兰南岛旅行网页信息架构与页面结构

这份文档用于调整网页内容。页面本身是手机优先的单页旅行工具，核心内容来自 `trip-data.json`，其中大部分数据由 `scripts/generate-new-zealand-trip-data.py` 从 `../行程/新西兰行程_V2.xlsx` 生成。

## 使用场景

- 分享到微信或旅行群，同行人用手机直接打开。
- 出发前确认航班、每日行程、餐厅预订、门票活动、取还车、待办事项。
- 旅行中快速展开当天安排，点地点按钮打开地图。
- 旅行中临时编辑每日行程和自驾信息，编辑内容保存在当前浏览器。
- 旅行中记录每一项花了多少钱，不做同行人分账。

## 页面总览

```text
┌────────────────────────────┐
│ 顶部导航                    │  常驻
│ 旅行信息菜单      记账入口   │
├────────────────────────────┤
│ 旅行封面                    │
│ 新西兰南岛                  │
│ 9月25日 - 10月7日 · 13天    │
├────────────────────────────┤
│ 航班行程                    │
│ 横向滑动航班卡  1 / 3       │
├────────────────────────────┤
│ 本次旅程                    │
│ 新加坡 / 新西兰南岛地图 Tab  │
│ 日期 Tab：09/25 ... 10/07   │
├────────────────────────────┤
│ 每日行程                    │
│ D1 折叠卡                   │
│ D2 折叠卡 + 预订/门票状态    │
│ ...                         │
│ D13 折叠卡                  │
├────────────────────────────┤
│ 自驾信息                    │
│ 取还车卡 + 倒计时           │
│ 取还车检查 / 订单保障 / 提醒 │
├────────────────────────────┤
│ 旅行前准备                  │
│ ❄️ / ⛰️ / 🎵 三组 To Do      │
├────────────────────────────┤
│ 页脚                        │
└────────────────────────────┘

记账是独立视图：
┌────────────────────────────┐
│ 记账                        │
│ 花费录入 / 按币种合计        │
└────────────────────────────┘
```

## 信息架构

```text
旅行网页
├─ 顶部导航
│  ├─ 旅行信息
│  │  ├─ 航班
│  │  ├─ 路线
│  │  ├─ 行程
│  │  ├─ 自驾
│  │  └─ Todo
│  └─ 记账
├─ 旅行封面
│  ├─ 目的地
│  ├─ 英文副标题
│  └─ 日期与天数
├─ 航班行程
│  ├─ 上海 -> 新加坡
│  ├─ 新加坡 -> 基督城
│  └─ 基督城 -> 新加坡 -> 上海
├─ 本次旅程
│  ├─ 新加坡城市路线示意
│  ├─ 新西兰南岛自驾路线示意
│  ├─ 每日路线 Tab
│  └─ 地点弹窗与 Google Maps 入口
├─ 每日行程
│  ├─ 13 天折叠卡
│  ├─ 每天时间轴
│  ├─ 单条行程本地编辑
│  ├─ 地点地图按钮
│  ├─ 餐厅预订、活动预约、门票提醒
│  └─ 费用标签
├─ 自驾信息
│  ├─ RaD Car Hire 订单摘要
│  ├─ 取车倒计时
│  ├─ 还车截止倒计时
│  ├─ 订单摘要本地编辑
│  ├─ 取还车检查本地编辑
│  ├─ 订单保障本地编辑
│  └─ 驾驶提醒本地编辑
├─ 旅行前准备
│  ├─ ❄️ 待办
│  ├─ ⛰️ 待办
│  └─ 🎵 待办
└─ 记账
   ├─ 金额
   ├─ 币种
   ├─ 分类
   └─ 事项清单
```

## 模块删减建议

当前 6 个模块都开启。开关在 `trip-data.json > config.modules`。

| 模块 | 当前状态 | 页面位置 | 建议 |
|---|---:|---|---|
| `flights` | 开启 | 航班行程 | 保留，适合出发前和转机查看 |
| `overview` | 开启 | 本次旅程地图 | 保留，适合同行人快速理解路线 |
| `itinerary` | 开启 | 每日行程 | 必须保留，是核心内容 |
| `driving` | 开启 | 自驾信息 | 保留，取还车和驾驶提醒很实用 |
| `todo` | 开启 | 旅行前准备 | 保留，可继续增删待办 |
| `ledger` | 开启 | 记账独立视图 | 如不打算旅行中记账，可关闭 |

关闭模块示例：

```json
"modules": {
  "flights": true,
  "overview": true,
  "itinerary": true,
  "todo": true,
  "driving": true,
  "ledger": false
}
```

关闭地图或航班后，需要重新运行校验；关闭地图时不需要 `routeMap`。

## 页面结构与数据位置

### 1. 顶部导航

页面文件：`index.html`

```text
旅行信息 -> 航班 / 路线 / 行程 / 自驾 / Todo
记账 -> 进入 ledger 独立视图
```

显示逻辑来自：

- `trip-data.json > config.modules`
- `app.js > applyModuleConfig()`
- `site-navigation.js`

常见调整：

- 只想隐藏某个入口：改 `config.modules`。
- 想改导航文字：改 `index.html` 中对应 `<a>` 文本。

### 2. 旅行封面

页面展示：

```text
New Zealand South Island
新西兰南岛
9月25日 - 10月7日 · 13天
```

数据位置：

- `trip-data.json > metadata.title`
- `trip-data.json > trip.heroTitle`
- `trip-data.json > trip.heroEyebrow`
- `trip-data.json > trip.startDate`
- `trip-data.json > trip.endDate`
- `trip-data.json > trip.dayCount`
- `assets/images/hero-new-zealand-lake.png`

推荐改法：

- 改大标题：改 `trip.heroTitle`
- 改英文副标题：改 `trip.heroEyebrow`
- 改网页标签标题：改 `metadata.title`
- 改封面背景图：替换 `assets/images/hero-new-zealand-lake.png`，或在 `styles.css` 的 `.hero` 中改图片路径

### 3. 航班行程

页面展示：

```text
FLIGHT 01 / 03
PVG 上海 -> SIN 新加坡

FLIGHT 02 / 03
SIN 新加坡 -> CHC 基督城

FLIGHT 03 / 03
CHC 基督城 -> SIN 新加坡 -> PVG 上海
```

数据位置：

- `trip-data.json > flightJourneys[]`
- `trip-data.json > flights[]`

每段航班关键字段：

```json
{
  "id": "flight-sq831",
  "journeyId": "journey-shanghai-singapore",
  "sequence": 1,
  "airline": { "nameZh": "新加坡航空" },
  "flightNumber": "SQ831",
  "departure": {
    "airportCode": "PVG",
    "city": "上海",
    "date": "2026-09-25",
    "time": "14:30",
    "utcOffset": "+08:00"
  },
  "arrival": {
    "airportCode": "SIN",
    "city": "新加坡",
    "date": "2026-09-25",
    "time": "20:20",
    "utcOffset": "+08:00"
  }
}
```

注意：

- `journeyId` 必须能对应 `flightJourneys[].id`。
- 同一个 Journey 下多段航班用 `sequence` 排序。
- 新西兰夏令时使用 `+13:00`，中国和新加坡使用 `+08:00`。

### 4. 本次旅程地图

页面展示：

```text
新加坡 | 新西兰南岛
总览
新加坡：09/25 09/26 10/06 10/07
新西兰南岛：09/27 09/28 09/29 10/02 10/03 10/04 10/05 10/06
路线示意图
```

输入数据位置：

- `trip-data.json > map.regions[]`
- `trip-data.json > map.places[]`
- `trip-data.json > map.routes[]`
- `trip-data.json > map.dailyRoutes[]`

派生数据位置：

- `trip-data.json > routeMap`
- `trip-data.json > metadata.assets.routeMaps`

不要手写 `routeMap`。它由下面命令生成：

```bash
npm run build:map
```

新增地图地点最小示例：

```json
{
  "id": "new-place",
  "name": "Place Name",
  "nameZh": "中文名",
  "countryCode": "NZ",
  "mapRegionId": "new-zealand",
  "geo": { "lat": -44.0, "lng": 170.0 },
  "query": "Place Name, New Zealand",
  "overview": true
}
```

新增每日路线示例：

```json
{
  "day": 9,
  "placeIds": ["wanaka", "lake-pukaki", "mt-cook", "twizel"],
  "scheduleItems": []
}
```

注意：

- `placeIds` 里的每个 ID 必须存在于 `map.places[]`。
- 地图按 `mapRegionId` 拆成新加坡和新西兰南岛两个区域。
- 上海只保留在航班和每日行程里，不做单独地图。
- 总览最多优先展示核心地点；每日 Tab 会显示当天更多地点。

### 5. 每日行程

页面展示：

```text
DAY 01 · 9月25日
上海出发，经新加坡入境
上海 -> 新加坡

展开后：
12:30 [卡片] ✈️ 抵达机场 · 编辑 / 删除 / 上移 / 下移
14:30 [卡片] 飞新加坡 · 交通 · 路程约5H50
...
```

数据位置：

- `trip-data.json > days[]`

每天结构：

```json
{
  "id": "day-03",
  "day": 3,
  "date": "2026-09-27",
  "title": "抵达基督城，取车后前往 Timaru",
  "locations": ["基督城", "Wigram", "Timaru"],
  "schedule": [
    {
      "id": "d03-01",
      "time": "10:30",
      "type": "attraction",
      "title": "Christchurch Airport",
      "subtitle": "抵达基督城",
      "icon": "✈️",
      "displayCategory": "traffic",
      "routeDuration": "",
      "itemDuration": "",
      "navigationQuery": "Christchurch Airport",
      "navigationLabel": "Christchurch Airport",
      "costs": { "cny": "", "nzd": "", "usd": "" },
      "note": "入境、取行李",
      "reserved": false,
      "text": "Christchurch Airport｜基督城；入境、取行李",
      "placeId": "基督城"
    }
  ],
  "notes": ["住宿：34D Jellicoe Street, Timaru..."],
  "costReferences": []
}
```

常见调整：

- 改折叠卡标题：改 `days[].title`
- 改折叠卡路线摘要：改 `days[].locations`
- 改某条行程卡：改 `days[].schedule[]`
- 增加费用标签：改 `days[].costReferences[]`
- 增加住宿备注：改 `days[].notes[]`
- 页面上点击行程卡的“编辑”，可以修改时间、关键地点/标题、项目/副标题、标题图案、路程耗时、项目耗时、导航、RMB/NZD/USD 花销、备注和预订状态。
- 生成数据时，Excel 的“关键地点”对应 `schedule[].title`，Excel 的“项目”对应 `schedule[].subtitle`。
- `displayCategory` 仅作为旧数据兼容和默认图案兜底，不在页面上展示或编辑。
- 页面上新增、删除、上移、下移行程卡后，会把当天完整行程保存到当前浏览器 LocalStorage，不会自动写回 Excel 或 `trip-data.json`。

行程项类型建议：

| 类型 | 用途 |
|---|---|
| `flight` | 飞机、机场、转机 |
| `drive` | 自驾、停车、取还车 |
| `transfer` | 接驳、地铁、去机场 |
| `restaurant` | 早餐、午餐、晚餐、小吃、咖啡 |
| `attraction` | 景点、参观、活动 |
| `walk` | 徒步、步行路线 |
| `check-in` | 入住、休整 |
| `check-out` | 退房、寄存行李 |

### 6. 预订与门票提醒

门票不是独立页面模块，它跟随“每日行程”显示。

数据位置：

- `trip-data.json > ticketPlanning.items[]`

当前用途：

- 餐厅预订提醒
- Ice Bar 等活动预约
- Skyline / Luge
- Air Safaris
- Lake Tekapo 观星
- 已打勾项目状态

结构示例：

```json
{
  "id": "ticket-skyline-luge",
  "day": 6,
  "category": "ticket",
  "name": "Skyline 缆车 + Luge",
  "requirement": "advance-required",
  "guidance": ["建议提前一天购买；4:30 前上缆车"],
  "officialUrl": "https://queenstown.skyline.co.nz/",
  "scheduleMatchTerms": ["skyline", "Luge", "缆车"]
}
```

字段说明：

- `day`：显示在哪一天。
- `category`：区分文案和颜色。
  - `restaurant-booking`：餐厅订位，显示“待预订 / 已订位”。
  - `activity-booking`：活动预约，显示“待预约 / 已预约”。
  - `ticket`：门票或体验票，显示“待购票 / 已购票”。
- `name`：卡片标题。
- `requirement`：状态文案。
  - `advance-required`：需提前购票 / 订位 / 预约
  - `advance-recommended`：建议预约或订位
  - `needs-confirmation`：方式待确认
- `purchaseStatus: "purchased"`：默认显示已准备。
- `scheduleMatchTerms`：匹配到对应行程项时显示在该行下面。
- `officialUrl`：打开官方页面。

### 7. 自驾信息

页面展示：

```text
RaD Car Hire Christchurch
取车：9月27日 11:00
还车：10月6日 09:00
车型：2025 Toyota Rav4 Hybrid AWD
费用：NZD 1230.00
```

数据位置：

- `trip-data.json > groundTransport.rentalCar`
- `trip-data.json > groundTransport.rentalChecklist[]`
- `trip-data.json > groundTransport.drivingNotes[]`
- `trip-data.json > groundTransport.drivingReferenceLinks[]`

改租车信息：

```json
"rentalCar": {
  "company": "RaD Car Hire Christchurch",
  "bookingNumber": "",
  "vehicle": {
    "example": "2025 Toyota Rav4 Hybrid AWD",
    "class": "SUV / Hybrid AWD"
  },
  "pickup": {
    "date": "2026-09-27",
    "time": "11:00"
  },
  "dropoff": {
    "date": "2026-10-06",
    "time": "09:00"
  }
}
```

隐私建议：

- 当前网页没有嵌入租车 PDF 原件。
- 如准备公开部署，建议不要放邮箱、手机号、完整住址、完整订单截图。
- 本地自用可以保留更多细节；公开分享版建议另做脱敏版本。
- 页面上的“编辑自驾信息”和“编辑本栏”用于旅行中临时调整；编辑保存在当前浏览器 LocalStorage，不会自动写回 Excel 或 `trip-data.json`。

### 8. 旅行前准备

页面展示：

```text
0 / 8
❄️ 3 | ⛰️ 3 | 🎵 2
[ ] 当前 tab 的准备事项
```

初始数据位置：

- `trip-data.json > preTrip.todoGroups[]`
- `trip-data.json > preTrip.packingItems[]`

用户在页面里新增或勾选的内容：

- 保存在当前浏览器 LocalStorage。
- 不会自动写回 `trip-data.json`。

改初始待办示例：

```json
{
  "id": "todo-01",
  "groupId": "snow",
  "text": "新加坡入境前 3 天填写入境卡",
  "completed": false
}
```

### 9. 记账

页面入口：

- 顶部导航右侧“记账”
- URL hash：`#ledger`

数据状态：

- 默认使用浏览器本地存储。
- 不需要数据库。
- 换设备不会自动同步。
- 不需要添加同行人，也不做谁垫付、谁分摊。
- 只记录某一项花了多少钱，并按币种显示合计。

如只想做攻略页，不需要记账：

```json
"ledger": false
```

如以后需要多设备共享记账，才考虑 Cloudflare D1。

## 内容修改优先级

推荐按下面顺序修改，最稳：

1. 修改原始 Excel：`../行程/新西兰行程_V2.xlsx`
2. 重新生成数据：

```bash
python3 scripts/generate-new-zealand-trip-data.py
```

3. 重建地图：

```bash
npm run build:map
```

4. 校验：

```bash
npm run validate
```

5. 本地预览：

```bash
npm run preview
```

如果只是小改标题、To Do、门票提醒，也可以直接改 `trip-data.json`。

## 文件职责

| 文件 | 用途 | 通常是否修改 |
|---|---|---:|
| `trip-data.json` | 页面所有旅行内容 | 经常 |
| `scripts/generate-new-zealand-trip-data.py` | 从 Excel 生成页面数据 | 偶尔 |
| `index.html` | 页面骨架和模块容器 | 少改 |
| `styles.css` | 旅行页样式 | 需要设计调整时改 |
| `app.js` | 航班、行程、自驾、Todo 渲染逻辑 | 少改 |
| `overview-map.js` | 地图 SVG 渲染 | 尽量不改 |
| `route-ui.js` | 地图交互、日期 Tab、弹窗 | 尽量不改 |
| `ledger.js` | 记账逻辑 | 需要改记账功能时改 |
| `ledger.css` | 记账样式 | 需要改记账视觉时改 |
| `local-preview-server.mjs` | 本地预览服务器 | 不改 |

## 当前内容清单

| 内容 | 数量 |
|---|---:|
| 行程天数 | 13 天 |
| 航班组 | 3 组 |
| 航班段 | 4 段 |
| 每日行程项 | 来自 Excel 全量提取 |
| 门票/预订提醒 | 11 项 |
| 初始 To Do | 8 项 |
| To Do 分组 | 3 组 |
| 新加坡地图地点 | 8 个 |
| 南岛地图地点 | 16 个 |
| 新加坡地图日期路线 | 4 天 |
| 南岛地图日期路线 | 8 天 |

## 调整检查清单

修改后至少检查：

- 首页标题是否还是“新西兰南岛”。
- 航班卡数量和顺序是否正确。
- 地图总览是否能显示。
- 每日行程折叠卡是否能展开。
- 门票提醒是否出现在正确日期。
- 自驾取还车时间是否正确。
- To Do 是否能勾选和新增。
- 记账入口是否能打开。
- 控制台没有明显错误。

## 适合删减的内容

如果要做“发给同行人的轻量版”，优先删减：

- 每条行程中过长的攻略备注。
- 部分餐厅备选项。
- 费用标签。
- 记账模块。
- 过细的停车说明。

建议保留：

- 航班。
- 每日标题和关键路线。
- 取还车时间。
- 住宿地址。
- 需要提前预约的门票/餐厅/活动。
- 新西兰自驾提醒。
